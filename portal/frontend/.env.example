VITE_API_BASE_URL=http://localhost:8080
# IS's own hosted self-registration page. Blank until IS is configured (Phase 1).
VITE_IS_SIGNUP_URL=
# Nominee Service - called directly by the frontend for anything nominee-related,
# never through the BFF. Validates the same split access token as the BFF.
VITE_NOMINEE_SERVICE_BASE_URL=http://localhost:8082
