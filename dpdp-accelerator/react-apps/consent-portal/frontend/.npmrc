# The upstream portal is developed with pnpm, which does not enforce peer
# ranges. This webapp is built by Maven with npm, which does, and
# @wso2/oxygen-ui pins an exact React patch version that the project's own
# react dependency is one patch ahead of. The two are compatible in practice;
# this keeps `npm install` from refusing the tree over that pin.
legacy-peer-deps=true
